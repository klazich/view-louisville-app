{
  "name": "view-louisville-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "flow-bin": "^0.73.0",
    "husky": "^0.14.3",
    "lint-staged": "^7.1.2",
    "prettier": "^1.13.2",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,json,css}": [
      "prettier --write",
      "git add"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "scripts": {
    "precommit": "lint-staged",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "flow": "flow",
    "prettier": "prettier --write src/**/*.{js,jsx,json,css}"
  }
}
